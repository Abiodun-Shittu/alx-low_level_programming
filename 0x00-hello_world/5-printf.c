#include <stdio.h>

/**
 * main- C program that prints to the standard output followed by a new line
 * Return: Always Zero
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
